# Print Bar App

React web app shell for a local LAN printing cockpit.

## Commands

```bash
npm install
npm run dev
npm run build
```

Milestone 1 is a mocked frontend experience. Backend integration with
`local_printer_api` is intentionally deferred to the next milestone.
